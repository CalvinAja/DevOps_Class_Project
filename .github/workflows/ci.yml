name: CI Pipeline

on:
  push:
    branches: [latihan]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Build image
        run: docker build -t calvinaja/webserver:latest .
      
      - name: Push image
        run: docker push calvinaja/webserver:latest
      
      # - name: Deploy html
      #   uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: ${{ secrets.GITHUB_TOKEN }}
      #     firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DEVOPS_BFE04 }}
      #     channelId: live
      #     projectId: devops-bfe04
